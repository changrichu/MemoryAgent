# 安全策略

## 支持的版本

| 版本 | 支持 |
|------|------|
| 0.1.x | ✅ |
| < 0.1 | ❌ |

## 报告安全漏洞

请**不要**通过公开 Issue 报告安全漏洞。

请发邮件到:your-email@example.com

我们会:
- 24 小时内回复确认
- 评估严重程度并修复
- 修复后公开致谢(可选)

## 已知安全考虑

### 1. Prompt Injection

LLM 容易被恶意输入劫持,本项目:
- ⚠️ 默认**不**做输入清洗
- 建议在 API 网关层加入 prompt 注入检测
- 关键场景(如涉及 SQL/代码执行)必须二次校验

### 2. 数据隐私

- 用户对话内容默认保留,可通过 `/memory/clear/{user_id}` 一键清除
- GDPR 合规支持
- 生产环境建议加密存储对话 + 脱敏展示

### 3. API Key 泄露

- `.env` 已加入 `.gitignore`
- 切勿提交真实 API Key 到 git
- CI/CD 建议用 Secrets 注入,不要硬编码

### 4. DOS / 滥用

当前未做严格限流,生产建议:
- API 网关限流(QPS 限制)
- 单用户 Token 配额
- 异常行为检测(异常高频调用)
